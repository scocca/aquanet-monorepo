/*import React, { useState } from 'react';
import { GoogleTranslateElement } from 'react-google-translate-component';

const Traduccion = () => {
  const [isGoogleTranslateLoaded, setIsGoogleTranslateLoaded] = useState(false);

  const handleGoogleTranslateLoad = () => {
    setIsGoogleTranslateLoaded(true);
  };

  return (
    
    <div id="google_translate_element">
    <div>
      {isGoogleTranslateLoaded && (
        <GoogleTranslateElement
          pageLanguage={'en'} 
        />
       
      )}
</div>
      <script
        type="text/javascript"
        src="https://translate.google.com/translate_a/element.js?cb=handleGoogleTranslateLoad"
        async={true}
      />
    </div>
  );
};

export default Traduccion;*/
